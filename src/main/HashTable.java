/*
Goal:  Implement a hash table - using an array of linked lists.
        Each key will be hashed to an index value within the (predetermined) size of the array.
        Each array location will contain a linked list of (duplicate key) values.
 */
public class HashTable<TKey, TValue>
{
    private static final int _buckets = 10;

    public void Add(TKey key, TValue value)
    {

    }
}
